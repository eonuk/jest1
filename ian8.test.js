

const inverse = (a) => 1/a;

describe('my tests', function() {

  describe('my tests', function() {
    test('inverse 1', () => {
      expect(inverse(4)).toBe(0.25);
    });

    test('inverse 2', () => {
      expect(inverse(10)).toBe(0.1);
    });
  });

});


