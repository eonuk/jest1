

const sqrt = (a) => Math.sqrt(a);

describe('my tests', function() {

  describe('my tests', function() {
    test('sqrt 1', () => {
      expect(sqrt(9)).toBe(3);
    });

    test('sqrt 2', () => {
      expect(sqrt(16)).toBe(4);
    });
  });

});


